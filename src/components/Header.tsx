import './Header.css';

export function Header() {
  return (
    <header className="app-header">
      <h1>GitLab CI Validator &amp; Simulator</h1>
      <p>Validate and simulate your .gitlab-ci.yml locally — no API calls required.</p>
    </header>
  );
}
